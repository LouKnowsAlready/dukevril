<%= form_with(model: academy, local: true) do |form| %>
  <% if academy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(academy.errors.count, "error") %> prohibited this academy from being saved:</h2>

      <ul>
      <% academy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :academy_name %>
    <%= form.text_field :academy_name, id: :academy_academy_name %>
  </div>

  <div class="field">
    <%= form.label :academy_instructor %>
    <%= form.text_field :academy_instructor, id: :academy_academy_instructor %>
  </div>

  <div class="field">
    <%= form.label :academy_country %>
    <%= form.text_field :academy_country, id: :academy_academy_country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
